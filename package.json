{
    "name": "coli-workspace",
    "version": "0.1.0",
    "license": "MIT",
    "private": false,
    "workspaces": [
        "packages/*",
        "editor",
        "example"
    ],
    "scripts": {
        "lerna": "lerna",
        "build": "lerna run build",
        "lerna:version": "lerna version --no-private",
        "publish:all": "yarn lerna:version && lerna publish",
        "bootstrap": "lerna bootstrap",
        "editor": "yarn workspace editor run dev",
        "build:editor": "yarn workspace editor run build"
    },
    "devDependencies": {
        "lerna": "^5.3.0"
    }
}
